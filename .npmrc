# Allow legacy peer deps to handle genkit version conflicts
legacy-peer-deps=true
strict-peer-deps=false

# Allow missing peer dependencies for specific packages
peer-dependency-rules.ignore-missing=@dataconnect/generated

